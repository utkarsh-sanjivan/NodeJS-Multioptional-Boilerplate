# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Node.js CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies for Javascript one
      run: npm install

    - name: Typescript Test
      run: node ./bin/createNodeApp.js --folder node-ts-example && cd node-ts-example && npm run test

    # - name: Install dependencies for Javascript one
    #   run: cd javascript/v1 && npm install

    # - name: Run the Tests
    #   run: cd javascript/v1 && npm run test

    # - name: Build
    #   run: cd javascript/v1 && npm run build

    # - name: Eslint Checkout
    #   run: cd javascript/v1 && npm run eslint:check

    # - name: Prettier Check
    #   run: cd javascript/v1 && npm run prettier:check

    # - name: Install dependencies for Javascript one
    #   run: cd typescript/v1 && npm install

    # - name: Run the Tests
    #   run: cd typescript/v1 && npm run test

    # - name: Build
    #   run: cd typescript/v1 && npm run build

    # - name: Eslint Checkout
    #   run: cd typescript/v1 && npm run eslint:check

    # - name: Prettier Check
    #   run: cd typescript/v1 && npm run prettier:check